import { ISearchOptions } from '../.ifaces/ISearchOptions';

export const searchShowInOroro = async (title: string, options: ISearchOptions) => {
  console.info('searchShowInOroro', title, options);
  return [];
};
