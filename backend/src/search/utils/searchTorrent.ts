import { ISearchOptions } from '../.ifaces/ISearchOptions';

export const searchShowInTorrent = async (title: string, options: ISearchOptions) => {
  console.info('searchShowInTorrent', title, options);
  return [];
};
